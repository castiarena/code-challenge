export default class Application {
    constructor(host, appdex, release, contributors) {
        this.appdex = appdex;
        this.release = release;
        this.contributors = contributors;
        this.host = host;
    }
}